# خطة نقل وتغليف تطبيق "نبض الملاعب"

مرحباً بك! هذه الوثيقة هي دليلك ومرجعك الدائم لنقل المشروع إلى GitHub ومن ثم "تغليفه" ليصبح تطبيقًا جاهزًا للرفع على متاجر Google Play و Apple App Store.

---

### **لماذا نقوم بهذه الخطوة؟**

1.  **الاحتفاظ بالكود:** وضع الكود على GitHub يضمن حفظه في مكان آمن وخاص بك.
2.  **التطوير المستمر:** يمكنك بسهولة سحب التحديثات التي أقوم بها هنا ودمجها مع نسختك على GitHub.
3.  **التغليف للموبايل:** وجود الكود على جهازك المحلي هو الخطوة الأولى لتحويله إلى تطبيق موبايل حقيقي.

---

### **الجزء الأول: نقل الكود إلى GitHub (تقوم بها مرة واحدة)**

1.  **إنشاء حساب ومستودع (Repository) على GitHub:**
    *   اذهب إلى [GitHub.com](https://github.com) وأنشئ حسابًا مجانيًا.
    *   أنشئ مستودعًا **"خاصًا" (Private)** باسم واضح مثل `nabd-almalaeb-app`.

2.  **تنزيل الكود من بيئة التطوير هذه:**
    *   احصل على نسخة كاملة من ملفات المشروع (عادةً خيار "Download ZIP").

3.  **إعداد ورفع الكود إلى GitHub من جهازك:**
    *   بعد تنزيل المشروع، قم بفك ضغط الملف المضغوط.
    *   افتح نافذة الأوامر (Terminal/CMD) وانتقل إلى داخل مجلد المشروع.
    *   قم بتنفيذ الأوامر التالية واحدًا تلو الآخر (استبدل `YourUsername/YourRepoName.git` بالرابط من GitHub):

    ```bash
    git init -b main
    git add .
    git commit -m "Initial commit of Nabd Al Malaeb application"
    git remote add origin https://github.com/YourUsername/YourRepoName.git
    git push -u origin main
    ```

**🎉 تهانينا!** الآن الكود المصدري لتطبيقك موجود بشكل آمن على حسابك الخاص في GitHub.

---

### **الجزء الثاني: "تغليف" التطبيق للموبايل (باستخدام Capacitor)**

**Capacitor** هي الأداة التي ستحول تطبيق الويب الخاص بنا إلى تطبيق موبايل حقيقي (ملف APK/AAB) بدون شريط الرابط المزعج. هذه العملية هي مزيج من تشغيل أوامر جاهزة واستخدام واجهات رسومية. **لن تحتاج لكتابة أي كود برمجي بلغة Java أو Swift.**

**المتطلبات الأساسية (تثبتها مرة واحدة):**
1.  **Node.js:** تأكد من تثبيته على جهازك.
2.  **Android Studio:** قم بتحميله وتثبيته من موقع Google الرسمي. هذا ضروري لبناء تطبيقات أندرويد.
3.  **Xcode (اختياري):** إذا كنت تريد بناء تطبيق لآيفون، ستحتاج إلى جهاز Mac وتثبيت Xcode من App Store.

**الخطوات العملية لتغليف التطبيق:**

1.  **إضافة Capacitor إلى مشروعك (تقوم بها مرة واحدة):**
    *   في نافذة الأوامر داخل مجلد مشروعك، قم بتشغيل الأوامر التالية:

    ```bash
    # تثبيت أدوات Capacitor
    npm install @capacitor/core @capacitor/cli

    # تهيئة Capacitor في المشروع (سيطرح عليك بعض الأسئلة البسيطة)
    # App Name: Nabd Al Malaeb
    # App ID: com.nabd.app (أو أي اسم فريد من اختيارك)
    npx cap init
    ```

2.  **إضافة منصة أندرويد (تقوم بها مرة واحدة):**
    *   بعد ذلك، أضف "الغلاف" الخاص بأندرويد:

    ```bash
    # تثبيت حزمة أندرويد
    npm install @capacitor/android

    # إضافة مجلد مشروع أندرويد إلى تطبيقك
    npx cap add android
    ```

3.  **تحديث إعدادات Next.js (مهم جدًا وقمت به بالفعل):**
    *   في ملف `next.config.ts`، قمنا بإضافة `output: 'export'` لتصدير نسخة ثابتة من التطبيق، وهذا ضروري ليعمل داخل الغلاف.

4.  **دورة البناء والمزامنة (تكررها عند كل تحديث):**
    *   في كل مرة تريد فيها تحديث تطبيق الموبايل بتغييرات جديدة في الكود، اتبع هذه الدورة البسيطة:

    ```bash
    # 1. بناء نسخة الويب النهائية (ينشئ مجلد 'out')
    npm run build

    # 2. مزامنة التغييرات مع منصة أندرويد
    npx cap sync android
    ```

5.  **فتح وبناء التطبيق النهائي من Android Studio:**
    *   افتح مشروع الأندرويد في Android Studio بهذا الأمر:

    ```bash
    npx cap open android
    ```
    *   **لأول مرة فقط:** قد يطلب منك Android Studio تحميل بعض الأدوات (SDKs)، وافق على ذلك.
    *   من داخل Android Studio، يمكنك تشغيل التطبيق على محاكي أو جهاز حقيقي للاختبار.
    *   **للنشر على Google Play:**
        *   من قائمة `Build`، اختر `Generate Signed Bundle / APK...`.
        *   اختر `Android App Bundle (.aab)` وانقر `Next`.
        *   ستحتاج إلى إنشاء "مفتاح توقيع" (Signing Key) لمرة واحدة فقط. اتبع التعليمات لإنشاء مفتاح جديد واحفظه في مكان آمن جدًا (هذا هو توقيعك الرسمي كمطور).
        *   بعد توقيع التطبيق، سيقوم Android Studio بإنشاء ملف `.aab` النهائي.
        *   هذا الملف هو الذي تقوم برفعه إلى حسابك في Google Play Console.

**هذه هي الطريقة الاحترافية والمعتمدة لنشر تطبيقك. لن يظهر شريط الرابط أبدًا بهذه الطريقة.**
